# venv PyTorch (CPU). conda env `frameworks-ia`, Python 3.11.15 (Linux).
#
# Versiones EXACTAS instaladas y VERIFICADAS (2026-07): forward/backward de torch,
# carga de MobileNetV3 preentrenada, softmax->numpy, cv2/PIL/matplotlib y el estudio
# de Optuna (TPESampler + MedianPruner) corren de punta a punta con este stack.
#   torch 2.12.0+cpu  ·  torchvision 0.27.0+cpu  ·  numpy 2.4.4
#   matplotlib 3.10.9  ·  pillow 12.2.0
# Los builds torch/torchvision son +cpu (índice https://download.pytorch.org/whl/cpu);
# acá se fija la versión base y el sufijo +cpu se resuelve al instalar desde ese índice.
# COMPATIBILIDAD (clave): numpy 2.x SOLO funciona con torch >= 2.3. La combinación vieja
# (torch 2.2.2 + numpy 2.4) se rompía; por eso torch y numpy suben juntos, no por separado.
# El env de TensorFlow (tensorflow-ia) va con numpy 2.4.6 (misma serie 2.4.x, coherencia).
torch==2.12.0
torchvision==0.27.0
numpy==2.4.4
matplotlib==3.10.9
pillow==12.2.0
# OpenCV para el consumo por cámara (modelos/v4/camara_pt.py). NO la variante headless:
# camara_*.py usa cv2.imshow, que el paquete headless no trae. Instalado 5.0.0.93.
opencv-python==5.0.0.93
# Optuna para la búsqueda de hiperparámetros de la v6 (PyTorch/v6/06_scripts.py).
# 4.x trae TPESampler, MedianPruner y los suggest_* usados en el script, compatible con
# numpy 2.x.
optuna==4.9.0
# scikit-learn: lo usa optuna.importance (fANOVA) para la figura de importancia de v6
# (Figure_optuna_importancia_v6.png). Sin él v6 NO se rompe (try/except) pero omite esa
# figura. Arrastra scipy como dependencia. Verificado con numpy 2.4.4.
scikit-learn==1.9.0
# --- v9: exportación a ONNX (PyTorch/v9 -> app Ionic) ---
# torch.onnx.export ya viene con torch; estos dos son para VERIFICAR la exportación:
#   onnx        -> checker.check_model(): valida que el grafo esté bien formado.
#   onnxruntime -> corre el .onnx y permite comparar sus salidas contra las de PyTorch
#                  (export/exportar_onnx.py aborta si difieren más de 1e-4).
onnx==1.22.0
onnxruntime==1.28.0
# CUIDADO AL INSTALARLOS: `pip install onnx onnxruntime` SUBE numpy a 2.x y ROMPE este venv.
# El síntoma es "RuntimeError: Numpy is not available" al llamar torch.from_numpy(), porque
# torch 2.2.2 se compiló contra la ABI de numpy 1.x. Ya pasó una vez en este repo.
# Si ocurre, la reparación es una línea:
#     pip install "numpy==1.26.4"
# La alternativa de fondo sería subir a torch >= 2.3 (que sí soporta numpy 2.x), pero eso
# cambiaría el entorno con el que se entrenaron v1-v9 y sus resultados dejarían de ser
# reproducibles. Se prefiere fijar numpy.
