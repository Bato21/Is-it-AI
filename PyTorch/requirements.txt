# venv PyTorch (CPU). Python 3.11.9.
#
# Versiones EXACTAS probadas (2026-06, Windows 11):
#   torch 2.2.2+cpu  ·  torchvision 0.17.2+cpu  ·  numpy 1.26.4
#   matplotlib 3.11.0  ·  pillow 12.2.0
# torch 2.2.x mantiene el export móvil (lite interpreter) de exportar_movil.py.
# numpy/matplotlib/pillow se fijan iguales que en TensorFlow (coherencia).
torch==2.2.2
torchvision==0.17.2
numpy==1.26.4
matplotlib==3.11.0
pillow==12.2.0
# OpenCV para el consumo por cámara (modelos/v4/camara_pt.py). NO la variante headless:
# camara_*.py usa cv2.imshow, que el paquete headless no trae. Probado con 5.0.0.
opencv-python==5.0.0
